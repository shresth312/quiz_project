<nav class="sidebar-nav">

    <div class="logo">💰 FinQuiz</div>

    <NavLink class="nav-item" href="/" Match="NavLinkMatch.All">
        🏠 Home
    </NavLink>

    <NavLink class="nav-item" href="/quiz">
        🧠 Quiz
    </NavLink>

    <NavLink class="nav-item" href="/leaderboard">
        🏆 Leaderboard
    </NavLink>

    <NavLink class="nav-item" href="/rewards">
        🎁 Rewards
    </NavLink>

</nav>


<style>

    /* Sidebar container */
    .sidebar-nav {
        display: flex;
        flex-direction: column;
        height: 100vh;
        padding: 30px 20px;
        background: linear-gradient(180deg,#0f2027,#203a43,#2c5364);
        animation: slideIn 0.8s ease forwards;
    }

    /* Logo styling */
    .logo {
        font-size: 26px;
        font-weight: bold;
        color: white;
        margin-bottom: 40px;
    }

    /* Navigation links */
    .nav-item {
        color: white;
        text-decoration: none;
        font-size: 18px;
        padding: 12px 16px;
        border-radius: 12px;
        margin-bottom: 12px;
        transition: all 0.3s ease;
    }

        /* Hover animation */
        .nav-item:hover {
            transform: translateX(8px);
            background: rgba(255,255,255,0.15);
        }

        /* Active link highlight */
        .nav-item.active {
            background: white;
            color: #203a43;
            font-weight: 600;
        }

    /* KEYFRAME FIX (no Razor conflict) */
    @@keyframes slideIn {
        0% {
            transform: translateX(-100%);
            opacity: 0;
        }

        100% {
            transform: translateX(0);
            opacity: 1;
        }
    }

</style>